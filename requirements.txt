aioredis
alembic
celery[redis]
fastapi[all]
flower
httpx 
jwt
passlib[bcrypt]
psycopg2-binary
pytest 
python-jose[cryptography]
python-multipart
google-auth
redis
sqlmodel
sqlalchemy
ultralytics
uvicorn[standard]
shapely
tensorflow[and-cuda]
keras
mtcnn
pillow
opencv-python-headless
numpy
requests
transformers==5.2.0
timm
lime
scikit-image
# ── Audio deepfake detection (READ module) ──────────────────────────────────
# NOTE: torch / torchaudio are installed by the Dockerfile with --index-url
#       https://download.pytorch.org/whl/cu118 to ensure the CUDA variant.
#       Adding them here without that flag would pull CPU-only wheels.
librosa
soundfile
captum>=0.7.0
shap>=0.44.0
matplotlib
# ── XAI Techniques (Advanced) ───────────────────────────────────────────────
timeshap>=0.3.0
mediapipe>=0.10.0
alibi>=0.9.0
scikit-learn>=1.3.0
PyYAML>=6.0
# segment-anything is installed from source in Dockerfile (facebookresearch/segment-anything)
# dlib is optional — only needed if mediapipe is insufficient
# PDF Report Generation
fpdf2>=2.7.0